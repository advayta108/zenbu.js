# {{name}}

A Zenbu plugin scaffolded by `zen init {{name}}{{recipesFlag}}`.
{{recipesList}}

## Layout

```
{{name}}/
├── zenbu.plugin.json        # manifest
├── package.json             # deps
├── tsconfig.json
├── tsconfig.local.json      # gitignored — path maps for #registry/* and #zenbu/*
├── setup.ts                 # idempotent dep installer
└── src/services/{{name}}.ts # main service (RPC-accessible via rpc["{{name}}"])
```

Each recipe you selected layers additional files on top. See
`{{appDir}}/zenbu/packages/zen/templates/plugin/recipes/<name>/` for
the raw templates.

## Host app

This plugin is wired to `{{appDir}}`. `tsconfig.local.json` maps
`#registry/*` to `{{appDir}}/types/*`, and `zen link` writes registry
types into that same directory. To re-target a different app, regenerate
`tsconfig.local.json` (or set `devAppPath` in `zenbu.plugin.json`).

## Common edits

- **Add an RPC method.** Add a public method on a `Service` subclass; run
  `zen link` from this dir to refresh registry types. Hot-reloads on save.
- **Change the schema** (if you have the `db` recipe). Edit `src/schema.ts`,
  run `zen kyju generate --name <tag>`, then `zen link`.
- **Add a dep.** Edit `package.json`, run `zen setup --dir .` — the app
  shows a Relaunch dialog because `node_modules` can't hot-reload.

## References

- Architecture: `{{appDir}}/zenbu/DOCS.md`
- Recipes skill overview: `{{appDir}}/zenbu/skills/zenbu-codebase/SKILL.md`
