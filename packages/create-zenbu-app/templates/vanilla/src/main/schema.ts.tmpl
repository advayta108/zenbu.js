import { createSchema, z } from "@zenbujs/core/db"

export default createSchema({
  issues: z
    .array(
      z.object({
        id: z.string(),
        title: z.string(),
        createdAt: z.number(),
      }),
    )
    .default([]),
})
